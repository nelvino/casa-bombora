// Admin access is now handled by a signed cookie session in the Admin page.
// This middleware file is intentionally left empty to prevent any route-level blocking.
export const config = {
  matcher: [],
}
